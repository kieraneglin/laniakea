var Laniakea = require('./../laniakea.js');
var assert = require('assert');

describe('Laniakea', () => {
  beforeEach(() => {
    this.l = new Laniakea();
  });
  describe('renameFile', () => {
    describe('dryrun: true', () => {
      it('returns non-namespaced mock rename', () => {
        console.log(this.wow);
      });
    });
  });
});
